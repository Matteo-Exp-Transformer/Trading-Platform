# FOLLOW_UP — debiti tecnici e controlli differiti

> Cose rimandate a dopo: polish UI fuori scope, controlli da fare, refactor differiti. Ogni voce
> ha un ID `FU-NNN` citabile dai report. L'agente prepara-prompt cerca qui i follow-up rilevanti
> quando prepara un nuovo prompt.

| ID | Aperto il | Descrizione | Origine (report) | Stato |
|----|-----------|-------------|------------------|-------|
| FU-001 | 2026-06-30 | **M1 — Auth & RLS**: creare progetto Supabase, popolare `.env.local` reale, intervista → `context/AUTH_CONTEXT.md`, poi Login + policy RLS. (Supabase rimandato da M0.) | `_sessioni-lavoro/2026-06-30/Report-completamento-init-e-M0.md` | **fatto** (2026-06-30) — codice M1 completo, `npm run validate` verde, test RLS passato |
| FU-002 | 2026-06-30 | **Recupero password self-service**: «password dimenticata» via email. Rimandato a **dopo l'intervista cliente**. In demo il reset lo fa l'admin a mano. Richiede SMTP + email reali. | intervista M1 (giro 2) | aperto |
| FU-003 | 2026-06-30 | **Validazione email reale**: conferma email all'iscrizione/creazione. In demo disattivata (email finte/interne ammesse). Da attivare con FU-002 dopo l'intervista cliente. | intervista M1 (giro 2) | aperto |
| FU-004 | 2026-06-30 | **Leaked password protection** (Supabase Auth → HaveIBeenPwned): attivare l'interruttore nel pannello Auth. Unico advisor security residuo dopo l'hardening M1; bassa priorità in demo (account a mano, password deboli ammesse), da valutare prima del prodotto finale. | revisione M1 (advisor Supabase) | aperto |
| FU-005 | 2026-06-30 | **Policy Storage per allegati (M4)**: quando `messages.attachments` inizierà a contenere riferimenti a file su Supabase Storage, definire le policy RLS su Storage (bucket privato per utente). Predisposto ora con la colonna jsonb; policy da scrivere a M4 prima di caricare file. | chiusura M2 (Report-M2-db-chats-messages-rls.md) | **superata** (2026-07-01) — svolta M4: non si conservano immagini ma una **scheda JSON** dell'analisi in `messages.attachments`. Niente file su Storage → nessuna policy Storage da scrivere. Vedi `AGENTE_AI_SKILL §4-bis` |
| FU-006 | 2026-06-30 | **RULE mancante in DB_SUPABASE_SKILL**: le policy INSERT su tabelle figlie (es. messages) devono verificare anche l'ownership della tabella padre (es. chats), non solo `user_id`. Aggiungere come RULE esplicita nel §5 prima del prossimo task DB. | chiusura M2 — falla rilevata dal test chat-rls.test.js caso (d) | **fatto** (2026-06-30, revisione M2) — RULE §5 presente ed estesa anche a UPDATE |
| FU-007 | 2026-06-30 | **Policy UPDATE simmetrica a INSERT su messages**: la UPDATE non verificava l'ownership della chat padre → un utente poteva spostare un proprio messaggio in una chat altrui (impatto solo d'integrità, nessuna fuga dati: il messaggio resta filtrato per `user_id`). Chiusa con migrazione `m2_hardening_messages_update_parent_check` + test caso (e). | revisione M2 (questa sessione) | **fatto** (2026-06-30) — migrazione applicata, advisor pulito, test verde |

| FU-008 | 2026-06-30 | **M2 slice 2c — Sidebar/storico chat**: lista delle proprie chat ordinate per `updated_at desc`, click su riga → apre quella chat con i suoi messaggi. Esplicitamente fuori scope 2b. | chiusura M2 slice 2b | **fatto** (2026-06-30) — `Sidebar.jsx` (drawer a comparsa) + `listChats()`, wiring in `Chat.jsx` |
| FU-009 | 2026-06-30 | **M2 slice 2c — Rinomina chat**: l'utente può cambiare il titolo di una chat esistente (`update chats.title`). Esplicitamente fuori scope 2b. | chiusura M2 slice 2b | **fatto** (2026-06-30) — icona rinomina in `SidebarChatRow` + `updateChatTitle()` |
| FU-010 | 2026-06-30 | **M3 — Persistenza strutturata del contesto-form per l'AI**: in 2b il contesto del form (asset/stile/obiettivo/posizione) è salvato solo come riepilogo testuale nel primo messaggio (`buildSummary`). A M3 l'agente ha bisogno del contesto **strutturato** valido per tutta la chat (CHAT_ANALISI_CONTEXT §5/§6): decidere dove persisterlo (colonna su `chats` o tabella dedicata) prima di collegare il cervello. | chiusura M2 slice 2b (revisione Senior) | **fatto** (2026-06-30, M3) — colonna `form_context jsonb` su `chats` (migrazione `m3_chats_form_context`), salvata da `createChat`. Nota: in M3 il prompt usa il riepilogo testuale; la colonna è pronta per uso strutturato futuro |
| FU-011 | 2026-06-30 | **TEST VISTA su grafici reali (rischio #1)**: la catena legge davvero le immagini (verificato con smoke test), ma la **qualità della lettura** su screenshot reali (XAU 15m, NAS100, ecc.) non è ancora validata. Provare grafici veri, valutare se 2.5 Pro legge struttura/livelli/RSI correttamente e se il `thinkingBudget=4096` basta; decidere il comportamento sul primo errore di lettura. | chiusura M3 | **fatto** (2026-07-01) — analisi su grafici reali verificata dall'utente: la lettura funziona correttamente. Rischio #1 rientrato; M3 chiuso del tutto |
| FU-012 | 2026-06-30 | **Upload form — slot fissi per-timeframe + compressione**: in M3 l'upload è un picker generico (max 3, ≥1 obbligatorio) con hint testuale. CHAT_ANALISI_CONTEXT §4 prevede **slot fissi ed etichettati per timeframe** (decisionale obbligatorio) derivati da stile×obiettivo. Inoltre nessuna compressione lato client: screenshot grandi → payload grande (limite route 25mb). Da rifinire. | chiusura M3 | **fatto** (2026-07-01) — `screenshotSlots()` (slot per-timeframe con obbligatorietà da stile×obiettivo) + slot etichettati in `NewAnalysisForm`; `imageCompression.js` (`compressImageToDataUrl`, fallback all'originale, mai blocca); etichette allegati nel riepilogo (`buildSummary(attachedLabels)`) per vision più affidabile. +8 test client, validate verde |
| FU-013 | 2026-06-30 | **Caching esplicito gestito (leva futura)**: in M3 si usa solo il caching **automatico** di Gemini (kit come blocco fisso in testa). Quando i volumi cresceranno, valutare il caching esplicito gestito (cachedContent) per il blocco kit. Non in M3. | chiusura M3 | aperto |
| FU-014 | 2026-06-30 | **`GOOGLE_API_KEY` formato non-standard**: la chiave in `.env.local` inizia con `AQ.Ab8...` (non il classico `AIza...`); **funziona** nei test live ma il formato è inusuale (possibile token a scadenza). Verificare che sia una API key Gemini stabile prima di demo/prod. | chiusura M3 | **fatto** (2026-07-01) — chiarito dall'utente: `AQ.Ab8...` è il **nuovo formato ufficiale** delle Google API key (Google ha cambiato il formato), non un token a scadenza. Chiave valida e stabile; Gemini risponde correttamente. Nessun rischio |

| FU-015 | 2026-07-01 | **Segnalazione screenshot non valido**: emerso in QA M4 — caricando una foto (paesaggio 4K) al posto di un grafico, il modello ha capito che l'immagine non andava ma **non l'ha segnalato**, dando comunque la lettura. L'utente deve essere avvisato che quell'immagine è inutilizzabile e che l'analisi può essere incompleta. | QA M4 (utente, 2026-07-01) | **fatto** (2026-07-01) — direttiva `buildImageCheckInstruction()` nel turno immagini (kit intatto): segnala quale immagine non è valida + avvisa di analisi incompleta, poi procede; campo `avvisi` aggiunto alla scheda JSON. +2 test. **Verificato live (2026-07-01):** l'utente ha ricaricato la foto errata e l'agente l'ha segnalata correttamente. Vedi `AGENTE_AI_SKILL §4-bis` |

| FU-016 | 2026-07-01 | **Console super-admin**: UI riservata all'admin (te) per gestire gli utenti, **assegnare il modello AI per account** (oggi si fa a mano dal DB — vedi M6) e visualizzare le statistiche per-utente (analisi, uso, ecc.). Sostituirà la gestione manuale del modello introdotta in M6. Area «admin» non ancora prevista nel piano: definire scope e sicurezza (accesso solo admin) prima di costruirla. | intervista M6 (utente, 2026-07-01) | aperto |

| FU-017 | 2026-07-01 | **M7 — Verifica visiva live in browser reale**: in questa sessione ho verificato i valori dei token nel CSS compilato e la suite `npm run validate`/`npm run build`, ma non ho potuto aprire un browser reale (nessuno strumento di automazione — `chromium-cli`/Playwright — disponibile in questo ambiente). Da controllare a occhio: tema chiaro E scuro su Login/Chat (bolle, composer, form nuova analisi, dropzone screenshot)/Sidebar/Impostazioni, desktop e mobile; nessun flash all'avvio; disclaimer leggibile in entrambi i temi. | `_sessioni-lavoro/2026-07-01/Report-M7-estetica-slate-ciano.md` | aperto |

| FU-018 | 2026-07-01 | **Home — MarketStatusBar**: orologio live + stato mercati Londra/New York/Tokyo (pallino verde aperto / grigio chiuso). Richiede logica orari mercati. | intervista Home (utente) | **fatto** (2026-07-01) — stato mercati in alto a destra nella Home: `useMarketStatus.js` (orari LOCALI per piazza via `Intl`/fuso IANA, DST-safe, solo lun–ven) + `MarketStatus.jsx` (Londra/New York/Tokyo, pallino **verde** aperto / grigio chiuso — verde confermato dall'utente, confinato al micro-indicatore) + slot `right` in `AppHeader`. Da mobile i mercati sono in **riga orizzontale in alto, a tutta larghezza** (distribuiti, sopra il nome app); da desktop inline a destra dell'header. **Orologio live NON incluso** (utente: solo stato mercati) → resta come possibile FU. +11 test, validate verde |
| FU-019 | 2026-07-01 | **Home — MarketOverview**: griglia ~9 asset (BTC, ETH, Oro, S&P500, Nasdaq, EUR/USD, GBP/USD, Petrolio, DAX) con prezzo/variazione/sparkline. Serve **fonte dati reale** (niente mock dove ci sono dati veri). | intervista Home (utente) | **parcheggiata** (2026-07-01) — decisione Senior+utente: **fuori dalla demo**. Ricerca licenze: i dati **azioni/indici** real-time (S&P500, Nasdaq, DAX) richiedono licenze di *display* delle borse (costose, per-utente, reporting; consolidato real-time ~5 cifre/mese) → non vendibili senza contratti pesanti. **Forex** più libero (mercato decentralizzato). **Crypto (CoinGecko)** ha licenza commerciale pulita ma **a pagamento** (~$35/mese da piano Analyst/Lite in su, con obbligo di attribuzione "Data provided by CoinGecko"; vietata la ridistribuzione del feed grezzo). Regola utente = «se serve licenza commerciale, non integriamolo» → **non integrata**. Riprendibile in futuro come blocco **solo-crypto CoinGecko** (scelta consapevole di licenza economica). La card Home "Monitoraggio mercati" resta **descrittiva** (nessun ticker). |
| FU-020 | 2026-07-01 | **Home — TradingCalendar**: calendario mensile stile dashboard, giorno corrente evidenziato in accento. | intervista Home (utente) | aperto |
| FU-021 | 2026-07-01 | **Home — FeatureCards "panoramica app"**: card Analisi assistita · Memoria sessioni · Journal · Monitoraggio mercati (icona + descrizione + micro-hover). | intervista Home (utente) | **fatto** (2026-07-01) — `components/home/FeatureCards.jsx`: 4 card statiche e **descrittive** (nessun link/bottone), icone SVG inline (accento ciano), hover bordo ciano; Journal segnata "in arrivo" (niente finta pagina). Solo client, dati interni, **nessuna licenza di terzi**. +2 test, validate (190+78) + build verdi. Verifica visiva a occhio → con FU-017 |
| FU-022 | 2026-07-01 | **Home — ActiveSessionCard**: se esiste una sessione/chat aperta, card "riapri sessione" (asset, ultimo aggiornamento, stato). | intervista Home (utente) | **fatto** (2026-07-01) — `components/home/ActiveSessionCard.jsx` + hook `useActiveSession.js` (chat più recente via `listChats()`, RLS): mostra titolo · asset · "aggiornata {relativeDayLabel}" (helper `lib/dateFormat.js`), click → riapre la chat (`storico.selectChat`). **Se non c'è nessuna sessione, o durante il caricamento, o su errore → la card non compare** (mai rompe la Home). +4 test (card) +6 (dateFormat), validate+build verdi. Verifica visiva → con FU-017 |
| FU-023 | 2026-07-01 | **Pagina Journal** (funzionalità nuova): diario di trading **integrato con le analisi AI**. Decisioni fondanti (2026-07-01): **ibrido** (voci da analisi via «Salva nel journal» + manuali) · **con numeri trade** (entry/exit/R:R/P&L opzionale, disclaimer confermato) · **MVP completo**. Mappata in `context/JOURNAL_CONTEXT.md`. | intervista Home (utente) | **fatto** (2026-07-02) — MVP completo. DB: tabella `journal_entries` + RLS (migrazioni `m8_journal_entries_rls` + hardening search_path) + test isolamento `server/test/journal-rls.test.js`. Client: `lib/journalData.js` (CRUD) + `lib/journalFields.js` (opzioni/prefill/normalizzazione) + pagina `pages/Journal.jsx` (lista, filtri asset/esito, modale form, elimina) + `components/journal/{JournalEntryForm,JournalEntryCard}.jsx`. Integrazioni: rotta `/journal` (App.jsx), voce Sidebar, card Home ora **link reale** (FeatureCards), bottone «Salva nel journal» in Chat (pre-fill da `form_context` via `getChat`). +~35 test, validate (client 235 + server 110) e build verdi. Advisor security puliti. |
| FU-024 | 2026-07-01 | **Pagina Trading Live** (funzionalità nuova): scope/dati da definire. CTA Home collegabile quando esiste. | intervista Home (utente) | aperto |
| FU-025 | 2026-07-01 | **Home — font Space Grotesk** per i titoli (accento "premium" dal prompt). Opzionale; oggi font di sistema per coerenza con l'app. | intervista Home (utente) | aperto |

| FU-026 | 2026-07-02 | **Chiudere la registrazione Supabase (signup aperto)** — rilievo n.1 dell'audit sicurezza: `disable_signup=false` a livello Auth (verificato su `/auth/v1/settings`). Chiunque con la anon key (pubblica nel bundle, per design) può creare un account via API e consumare analisi → costi Gemini. **Azione utente sul pannello**: Authentication → Sign In / Providers → «Allow new users to sign up» OFF; nella stessa zona attivare anche la **leaked password protection** (chiude FU-004). Il rate limit introdotto oggi riduce il danno ma non chiude la porta. Poi ricontrollare gli advisor. | audit sicurezza 2026-07-02 | aperto |
| FU-027 | 2026-07-02 | **Manipolazione cronologia chat (finti messaggi assistant)**: un utente può inserire via API, col proprio token (RLS rispettata, `role` ammette 'assistant'), messaggi contraffatti nella propria chat; la storia manipolata arriva a Gemini e **aggira il classificatore d'ingresso** (Strato 2 legge solo l'ultimo messaggio utente) → vettore di estrazione del kit. Barriera residua: canary (Strato 4). Difesa vera = decisione di design: persistenza messaggi lato server (oggi LOCKED nel flusso client) **oppure** classificatore esteso alla storia. Trattare come mini-feature (contesto → piano → esecuzione). | audit sicurezza 2026-07-02 | aperto |
| FU-028 | 2026-07-02 | **Bundle client > 500 kB** (warning build con Vite 8, ~651 kB minificato): valutare code-splitting (dynamic `import()` per pagina) quando si lavorerà sulla performance. Solo warning, nessun blocco funzionale. | audit sicurezza 2026-07-02 (build post-upgrade) | aperto |
| FU-029 | 2026-07-02 | **Smoke test a video dopo l'upgrade Vite 8 / Vitest 4**: `validate` (235 client + 125 server) e `build` verdi, ma l'app non è stata provata in dev nel browser dopo il salto di major (HMR, proxy `/api`, login, analisi). Da fare a inizio prossima sessione (si aggancia a FU-017, verifica visiva). | chiusura sessione 2026-07-02 | aperto |
| FU-030 | 2026-07-02 | 🔴 **URGENTE — Repo GitHub PUBBLICO con `kit/` dentro**: l'API GitHub risponde 200 senza autenticazione → `Matteo-Exp-Transformer/Trading-Platform` è visibile a chiunque, **incluso il metodo Aware Trader** (`kit/`, LOCK «mai esposto») e la documentazione di prodotto. **Azione utente subito**: GitHub → repo → Settings → General → Danger Zone → «Change visibility» → **Private**. Poi valutare se considerare il kit compromesso (è nella cronologia pubblica da fine giugno) e, in prospettiva, spostarlo fuori dal repo (es. `_private/` o storage server). | audit sicurezza 2026-07-02 (verifica post-push) | aperto |

> Stati: `aperto` · `in corso` · `fatto` (con data di chiusura) · `scartato` (con motivo).
